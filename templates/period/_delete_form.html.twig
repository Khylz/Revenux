<form method="post" action="{{ path('app_period_delete', {'id': period.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette période ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ period.id) }}">
    <button class="btn">Supprimer</button>
</form> 